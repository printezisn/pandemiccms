<%
    content_for :title, _('Media | %{client}') % { client: current_client.name }
    content_for :description, _('Media of %{client}') % { client: current_client.name }
%>

<article class="card">
  <header class="card-header">
    <h1 class="card-header-title">
      <i class="fa-solid fa-image"></i>
      &nbsp;
      <%= _('Media') %>
    </h1>
  </header>
  <div class="card-content">
    <div class="smart-table" id="media-smart-table" data-use-history="true">
      <%= render partial: 'admin/media/media_table' %>
    </div>
  </div>
</article>