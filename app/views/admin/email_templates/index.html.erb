<%
    content_for :title, _('Email Templates | %{client}') % { client: current_client.name }
    content_for :description, _('Email templates of %{client}') % { client: current_client.name }
%>

<article class="card">
  <header class="card-header">
    <h1 class="card-header-title">
      <i class="fa-solid fa-envelope"></i>
      &nbsp;
      <%= _('Email Templates') %>
    </h1>
  </header>
  <div class="card-content">
    <div class="smart-table" id="email-templates-smart-table" data-use-history="true">
      <%= render partial: 'admin/email_templates/email_templates_table' %>
    </div>
  </div>
</article>